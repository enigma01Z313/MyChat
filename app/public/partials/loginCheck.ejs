<script>
(function() {
  const loggedUser = JSON.parse( localStorage.getItem('userData') )
  const {status: {code}, publicLock} = loggedUser

  if(
    window.location.href !== `${siteUrl}/setup` && 
    (code === 0 || publicLock === null))
    return window.location.replace(`${siteUrl}/setup`);
})()
</script>